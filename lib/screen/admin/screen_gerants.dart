import 'package:flutter/material.dart';

class ScreenGerants extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gérants'),
      ),
      body: Center(
        child: Text('Contenu de la page Gérants'),
      ),
    );
  }
}
